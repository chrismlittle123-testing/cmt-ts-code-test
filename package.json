{
  "name": "cmt-ts-code-test",
  "version": "1.0.0",
  "description": "Test suite for check-my-toolkit CODE domain features",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "knip": "knip"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "check-my-toolkit": "^1.4.0",
    "eslint": "^9.0.0",
    "knip": "^5.0.0",
    "prettier": "^3.2.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
